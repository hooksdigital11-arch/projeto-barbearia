import { getRevenueReport } from './src/features/reports/queries'

async function run() {
  // O test script no Node ambiente não tem o requireAdmin() ou o cookie.
  // Entao isso vai falhar se eu rodar direto no tsx.
}
